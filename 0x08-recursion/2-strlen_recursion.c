#include <stdio.h>
#include <unistd.h>
#include <string.h>

/**
 * _strlen_recursion - returns length
 * @s: string to count
 * Return: length
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
