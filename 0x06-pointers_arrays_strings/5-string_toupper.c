#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include "main.h"

/**
 * _strcat - appending src to dest
 * @str: parameter of interest
 * Return: 0
 */

char *string_toupper(char *)
{
	char str;

	printf("%s", toupper(str));

	return (0);
}
