##include <stdio.h>

/**
 * main - Prints the numbers from 1 to 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

        for (x = 0; x <= 9; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
